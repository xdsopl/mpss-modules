This is a port of the mic module for the Intel Xeon Phi Coprocessor for linux-3.14

mpss-modules-3.2.1 untested but compiles fine on linux-3.14.1

to install, edit (if needed) and execute "install.sh"

